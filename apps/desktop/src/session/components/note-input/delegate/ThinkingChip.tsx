import { motion } from "motion/react";  import { cn } from "@hypr/utils";  export function ThinkingChip() {   return (     <motion.div       initial={{ opacity: 0, scale: 0.95 }}       animate={{ opacity: 1, scale: 1 }}       exit={{ opacity: 0, scale: 0.95 }}       transition={{ duration: 0.15 }}       className={cn([         "relative flex items-center gap-1.5 overflow-hidden rounded-full px-3 py-1",         "bg-char-ink text-white",         "font-mono text-xs",         "select-none",       ])}     >       {/* shimmer sweep */}       <span         className="pointer-events-none absolute inset-0 -translate-x-full bg-gradient-to-r from-transparent via-white/10 to-transparent"         style={{ animation: "shimmer 1.6s ease-in-out infinite" }}       />       <span className="relative z-10 text-white/50">{"{ }"}</span>       <span className="relative z-10 text-white/80">thinking…</span>     </motion.div>   ); }