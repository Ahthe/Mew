import { CheckIcon } from "lucide-react"; import { motion } from "motion/react";  import { cn } from "@hypr/utils";  export function DelegatedResult({ text }: { text: string }) {   return (     <motion.div       initial={{ opacity: 0, clipPath: "inset(0 100% 0 0)" }}       animate={{ opacity: 1, clipPath: "inset(0 0% 0 0)" }}       exit={{ opacity: 0 }}       transition={{ duration: 0.45, ease: "easeOut" }}       className={cn([         "border-char-line max-w-[240px] rounded-xl border bg-white p-3 shadow-md",         "text-xs",       ])}     >       <div className="text-char-coral mb-2 flex items-center gap-1.5 font-mono">         <CheckIcon className="size-3" />         <span className="font-semibold">Delegated</span>       </div>       <p className="text-char-muted leading-snug">{text}</p>       <div className="mt-2.5 flex flex-col gap-1">         {/* TODO(phase2): replace with actual inserted sub-tasks */}         {["Draft email to @Artem", "Trim the launch checklist"].map((task) => (           <div             key={task}             className="text-char-muted-soft flex items-center gap-1.5 text-[10px]"           >             <span className="border-char-line size-3 rounded-sm border" />             {task}           </div>         ))}       </div>     </motion.div>   ); }