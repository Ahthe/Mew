import { ArrowRightIcon } from "lucide-react"; import { motion } from "motion/react";  import { cn } from "@hypr/utils";  export function DelegatePill({ onDelegate }: { onDelegate: () => void }) {   return (     <motion.button       initial={{ opacity: 0, x: 6 }}       animate={{ opacity: 1, x: 0 }}       exit={{ opacity: 0, x: 6 }}       transition={{ duration: 0.12, ease: "easeOut" }}       whileTap={{ scale: 0.95 }}       onClick={(e) => {         e.stopPropagation();         onDelegate();       }}       className={cn([         "flex items-center gap-1.5 rounded-full px-3 py-1",         "border-char-line border bg-white shadow-sm",         "text-char-muted font-mono text-xs",         "transition-colors duration-100",         "hover:border-char-coral/40 hover:text-char-coral hover:shadow-md",         "select-none",       ])}     >       <span className="text-char-coral/50">{"{ }"}</span>       Delegate       <ArrowRightIcon className="size-3 opacity-60" />     </motion.button>   ); }